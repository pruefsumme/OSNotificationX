# OSNotificationX Preview

Open `index.html` in a browser to preview the Notification Center styling with sample notifications, or render a PNG:

```sh
./preview/render-preview.sh
```

The preview intentionally mirrors the GTK CSS in `src/osnx-style.c` so visual changes can be compared quickly before reinstalling the panel plugin.
